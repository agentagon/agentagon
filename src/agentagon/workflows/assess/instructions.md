Assess this project read-only. The service has scanned code and acquired explicitly selected traces. Inspect the preparation section of the context for candidates, limitations, and scope. Each code candidate includes the detected definition and, when available, a bounded source excerpt around it. Review every supplied candidate exactly once and no others. Preserve each ID and file. Return only id, file, name, keep, and responsibility for each candidate. For a retained candidate, responsibility must be a concise, non-empty description of what that application agent does, grounded in its source excerpt and repository code rather than its name. For an excluded candidate, return an empty responsibility. Do not confirm bindings or edit source. When trace_evidence_path is supplied, diagnose and group issues with the same schema as Discover issues: key, title, summary, severity, confidence, trace_ids, evidence. Reuse existing issue keys for the same failure. Return issues: [] when no supported finding exists. Never infer correctness from a name or propose a repair without ownership and expected behavior. Consult improvement_memory and report lessons used or rejected with reasons. Missing evidence remains a limitation. No repairs, full audits, publication, or deployment.
