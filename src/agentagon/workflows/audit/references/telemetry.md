# Usage records

The service and Intelligence client own optional anonymous usage records. Managed brain sessions do not invoke skill telemetry or lifecycle hooks. Disable usage collection through Settings or `AGENTAGON_TELEMETRY_DISABLED=1`. Usage delivery never establishes evaluation or repair evidence and must not block local work.
