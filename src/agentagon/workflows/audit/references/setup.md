# Runtime prerequisites

Agentagon requires Python 3.12+ and a configured managed Codex or Claude backend. The service supplies the installed Python executable, private CLI prefix and resource paths. Full audits can inspect non-Git directories and uncommitted work. Evaluation and repair require clean committed inputs for isolated execution. If a prerequisite is missing, report it as a shared pending question or limitation.
