Observe the selected production traces read-only. The service owns measurement, comparisons, checkpoints and production statuses. Diagnose issues from trace_evidence_path with the Discover issues schema: key, title, summary, severity, confidence, trace_ids, evidence. Reuse existing issue keys for the same failure. Return issues: [] if no supported finding exists. Explicitly distinguish unknown behavior from success. Do not repair, change a measurement, declare production recovered, confirm ownership, or modify source. Consult improvement_memory and report lessons used or rejected with reasons. Only the selected evidence is in scope.

For accepted issue_recurrence measurements only, report explicit per-trace reviews in issue_checks: [{issue_id, trace_id, present, reason}]. Present is a boolean supported by the trace and the named issue's expected behavior; explain that evidence in reason. Omit unknown or unreviewed traces. An empty issues list does not establish absence. These reviews are measurements of the selected sample, never claims of universal recovery.
