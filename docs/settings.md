# Configure the local service

Settings select the managed brain and model, execution profiles, provider credential references and Intelligence preferences. Application-agent discovery is separate from coding-backend detection. Explicit invocation overrides project settings, which override user defaults. Credentials are references, not stored secret values.

[Dashboard](app.md) · [Workflows](workflows.md) · [MCP](mcp.md)
